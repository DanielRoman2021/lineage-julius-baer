# Production build (Cloudflare Pages) talks to the deployed Render backend.
# Local dev uses .env.local (http://localhost:8000), which is gitignored and
# only loaded by `next dev`, so this does not affect local development.
NEXT_PUBLIC_API_URL=https://lineage-api-h1gi.onrender.com
